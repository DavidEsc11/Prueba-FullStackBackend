function calculateSumayMultiplicar(summand1, summand2, multiplier) {
    let sum = summand1 + summand2;
    let product = sum * multiplier;
    let sineResult = Math.sin(product);
    return sineResult;
  }